{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "// ConnectionStrings:PetCareDb": "Do not commit a real password here. Set it via `dotnet user-secrets init` then `dotnet user-secrets set \"ConnectionStrings:PetCareDb\" \"<your-connection-string>\"`, or set the PETCARE_DB_CONNECTION environment variable. No hardcoded fallback is used.",
  "ConnectionStrings": {
    "PetCareDb": ""
  },
  "Cors": {
    "AllowedOrigins": ["http://localhost:5173"]
  },
  "// Jwt:Key": "Do not commit a real signing key here. Set it via `dotnet user-secrets set \"Jwt:Key\" \"<a long random secret>\"`, or the PETCARE_JWT_KEY environment variable. No hardcoded fallback is used.",
  "Jwt": {
    "Issuer": "PetCareApi",
    "Audience": "PetCareClient",
    "ExpiryMinutes": 60,
    "Key": ""
  }
}
