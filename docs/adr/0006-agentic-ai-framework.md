# ADR 0006 — Agentic AI Framework / Orchestration

**Status:** TODO — decision not yet made · **Date:** pending

## Context

The assignment requires an agentic AI component. Current state: the AI workflow screens are **monitoring UI only** — no agent, orchestration, model calls, or backend AI service exists.

## Options considered

- `[TO BE EVALUATED]` — e.g. semantic-kernel-style orchestration, LangChain, custom tool-calling pipeline
- `[TO BE EVALUATED]` — hosting location (inside `PetCare.Api` vs separate `backend/ai-service`)

## Decision

`[PENDING]` — no framework has been selected or implemented.

## Consequences

- `docs/` and README describe AI as UI-only — accurate today
- Do not record an evaluation or framework claim until the implementation exists
